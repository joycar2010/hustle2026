# Backup handling

The repository is public. Do not add plaintext `.env`, login passwords,
bridge API keys, or terminal credential files to this branch. Use the
encrypted artifact in `secrets/` and keep the local key offline. Rotate a
credential if the key is lost or exposed. Redacted JSON and task templates
retain paths, ports, account labels, and operational structure without
publishing secrets.
