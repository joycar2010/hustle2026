# C:\MT5Agent source snapshot

This directory contains the production management Agent source (`main_v3.py`), dependency pinning, and redacted configuration templates exported from `C:\MT5Agent` on `43.206.15.17`.

`config.template.json` and `instances.template.json` preserve the configuration shape only. API keys, database passwords, MT5 passwords, tokens, and login state were removed. The production `venv`, logs, caches, and runtime state are intentionally excluded.
