# MT5多账户容量规划分析报告

## 服务器配置

**AWS EC2 实例**：
- 实例类型：c6i.large
- CPU：2 vCPU (Intel Xeon Ice Lake, ~3.5 GHz)
- 内存：4GB RAM (4015 MB 可用)
- 操作系统：Windows Server 2022 Base
- 区域：东京 (ap-northeast-1)

**当前资源使用**：
- 总内存：4015 MB
- 已用内存：3755 MB (93.5%)
- 可用内存：260 MB (6.5%)

---

## MT5客户端资源消耗分析

### 单个MT5客户端

**实测数据**（当前1个MT5实例）：
```
进程名称：terminal64.exe
内存占用：31 MB
CPU占用：~1-2%（空闲时）
CPU占用：~5-10%（交易时）
网络连接：2-3个TCP连接
```

**资源分解**：
```
基础进程：~20 MB
图表和数据：~5 MB
连接和缓存：~6 MB
```

### 5个MT5客户端

**预估资源消耗**：
```
内存占用：31 MB × 5 = 155 MB
CPU占用（空闲）：1-2% × 5 = 5-10%
CPU占用（交易）：5-10% × 5 = 25-50%
网络连接：2-3 × 5 = 10-15个TCP连接
```

---

## 双平台账户资源消耗分析

### 单组账户（1个Binance + 1个Bybit MT5）

**后端服务资源**：
```
账户数据缓存：~5 MB
持仓数据：~2 MB
订单历史：~3 MB
WebSocket连接：~5 MB
总计：~15 MB/组
```

**策略执行资源**：
```
策略配置：~1 MB
阶梯进度：~0.5 MB
执行状态：~0.5 MB
总计：~2 MB/组
```

**数据库资源**：
```
账户记录：~1 KB
持仓记录：~2 KB/条
订单记录：~1 KB/条
价差历史：~200 bytes/条
```

**API调用频率**（每组）：
```
Binance账户查询：1次/10秒
Binance订单执行：按需（平均 0.1次/秒）
Bybit MT5查询：1次/10秒
Bybit MT5订单执行：按需（平均 0.1次/秒）
```

**总资源消耗（单组）**：
```
内存：~17 MB
CPU（空闲）：~2%
CPU（交易）：~5-8%
```

---

## 当前系统负载分析

### 基础服务

**Python后端（FastAPI）**：
```
进程内存：~200 MB
市场数据服务：~10 MB
WebSocket管理：~30 MB
数据库连接池：~30 MB
其他服务：~30 MB
总计：~300 MB
```

**Windows系统**：
```
系统进程：~1500 MB
系统缓存：~500 MB
其他服务：~200 MB
总计：~2200 MB
```

**数据库（PostgreSQL）**：
```
进程内存：~150 MB
数据缓存：~100 MB
连接池：~50 MB
总计：~300 MB
```

**当前总内存使用**：
```
Windows系统：2200 MB
Python后端：300 MB
PostgreSQL：300 MB
MT5客户端（1个）：31 MB
其他：924 MB
总计：3755 MB / 4015 MB (93.5%)
```

---

## 容量规划计算

### 场景1：5个MT5客户端（5组双平台账户）

**新增资源需求**：
```
MT5客户端：31 MB × 4 = 124 MB（新增4个）
账户数据：17 MB × 4 = 68 MB（新增4组）
总新增：192 MB
```

**预计总内存使用**：
```
当前使用：3755 MB
新增需求：192 MB
预计总计：3947 MB / 4015 MB (98.3%)
剩余可用：68 MB (1.7%)
```

**CPU使用率**：
```
当前CPU（空闲）：~15%
新增CPU（空闲）：~10%（4个MT5 + 4组账户）
预计总计：~25%

当前CPU（交易）：~30%
新增CPU（交易）：~30%（4个MT5 + 4组账户）
预计总计：~60%
```

**结论**：⚠️ **内存非常紧张（98.3%），可能不稳定**

---

### 场景2：10个MT5客户端（10组双平台账户）

**新增资源需求**：
```
MT5客户端：31 MB × 9 = 279 MB
账户数据：17 MB × 9 = 153 MB
总新增：432 MB
```

**预计总内存使用**：
```
当前使用：3755 MB
新增需求：432 MB
预计总计：4187 MB / 4015 MB (104.3%)
超出容量：172 MB
```

**结论**：❌ **内存不足，无法运行**

---

### 场景3：3个MT5客户端（3组双平台账户）

**新增资源需求**：
```
MT5客户端：31 MB × 2 = 62 MB
账户数据：17 MB × 2 = 34 MB
总新增：96 MB
```

**预计总内存使用**：
```
当前使用：3755 MB
新增需求：96 MB
预计总计：3851 MB / 4015 MB (95.9%)
剩余可用：164 MB (4.1%)
```

**CPU使用率**：
```
空闲时：~20%
交易时：~45%
```

**结论**：✅ **可以运行，但内存使用率较高**

---

## 内存优化分析

### 当前"其他"内存占用（924 MB）

**可能的占用**：
```
浏览器/前端开发工具：~200 MB
临时文件和缓存：~100 MB
未释放的Python对象：~50 MB
系统页面文件：~300 MB
其他进程：~274 MB
```

**优化潜力**：
```
关闭不必要的服务：~200 MB
清理临时文件：~100 MB
优化Python内存：~50 MB
总计可释放：~350 MB
```

**优化后可用内存**：
```
当前可用：260 MB
优化释放：350 MB
优化后可用：610 MB
```

---

## 最大容量计算

### 理想情况（优化后）

**可用内存**：610 MB

**单组账户消耗**：
```
MT5客户端：31 MB
账户数据：17 MB
总计：48 MB/组
```

**最大账户组数**：
```
可用内存：610 MB
单组消耗：48 MB
最大组数：610 / 48 ≈ 12组
```

**保留安全余量（20%）**：
```
安全余量：610 × 0.2 = 122 MB
可用内存：610 - 122 = 488 MB
最大组数：488 / 48 ≈ 10组
```

**结论**：✅ **优化后最多支持 10组双平台账户**

---

### 实际情况（未优化）

**可用内存**：260 MB

**最大账户组数**：
```
可用内存：260 MB
单组消耗：48 MB
最大组数：260 / 48 ≈ 5组
```

**保留安全余量（20%）**：
```
安全余量：260 × 0.2 = 52 MB
可用内存：260 - 52 = 208 MB
最大组数：208 / 48 ≈ 4组
```

**结论**：⚠️ **当前最多支持 4组双平台账户（含现有1组）**

---

## CPU容量分析

### 2 vCPU性能

**单核性能**：
```
基准性能：3.5 GHz Intel Ice Lake
单线程性能：~1000 PassMark分数
多线程性能：~2000 PassMark分数
```

**当前负载**：
```
系统进程：~10%
Python后端：~15%
数据库：~5%
MT5（1个）：~2%
总计：~32%（空闲时）
```

**5组账户负载**：
```
系统进程：~10%
Python后端：~20%
数据库：~8%
MT5（5个）：~10%
策略执行：~12%
总计：~60%（空闲时）
总计：~85%（交易时）
```

**10组账户负载**：
```
系统进程：~10%
Python后端：~25%
数据库：~12%
MT5（10个）：~20%
策略执行：~23%
总计：~90%（空闲时）
总计：~130%（交易时）❌ 超载
```

**结论**：
- ✅ 5组账户：CPU可以承受（60-85%）
- ❌ 10组账户：CPU会超载（90-130%）

---

## 网络容量分析

### 带宽需求

**单组账户**：
```
Binance API：~1 KB/10秒 = 0.1 KB/s
Bybit MT5：~0.5 KB/10秒 = 0.05 KB/s
WebSocket推送：~0.5 KB/s
总计：~0.65 KB/s
```

**5组账户**：
```
API调用：0.15 KB/s × 5 = 0.75 KB/s
WebSocket：0.5 KB/s × 5 = 2.5 KB/s
市场数据：5 KB/s（共享）
总计：~8.25 KB/s = 0.066 Mbps
```

**10组账户**：
```
API调用：0.15 KB/s × 10 = 1.5 KB/s
WebSocket：0.5 KB/s × 10 = 5 KB/s
市场数据：5 KB/s（共享）
总计：~11.5 KB/s = 0.092 Mbps
```

**结论**：✅ **网络带宽完全不是瓶颈**（12.5 Gbps可用）

---

## 数据库容量分析

### 写入负载

**单组账户**：
```
持仓更新：1次/30秒 = 0.033次/秒
订单记录：0.1次/秒（平均）
总计：~0.13次/秒
```

**5组账户**：
```
持仓更新：0.033 × 5 = 0.165次/秒
订单记录：0.1 × 5 = 0.5次/秒
价差历史：1次/秒（共享）
总计：~1.67次/秒
```

**10组账户**：
```
持仓更新：0.033 × 10 = 0.33次/秒
订单记录：0.1 × 10 = 1次/秒
价差历史：1次/秒（共享）
总计：~2.33次/秒
```

**结论**：✅ **数据库完全可以承受**（PostgreSQL可处理 1000+次/秒）

---

## 综合容量评估

### 当前配置（未优化）

| 账户组数 | 内存使用 | CPU使用（空闲） | CPU使用（交易） | 状态 |
|---------|---------|----------------|----------------|------|
| 1组（当前） | 93.5% | 32% | 50% | ✅ 正常 |
| 3组 | 95.9% | 45% | 65% | ✅ 可用 |
| 4组 | 97.3% | 52% | 75% | ⚠️ 紧张 |
| 5组 | 98.3% | 60% | 85% | ⚠️ 很紧张 |
| 10组 | 104.3% | 90% | 130% | ❌ 超载 |

**结论**：
- ✅ **最多支持 3-4组账户**（安全运行）
- ⚠️ **5组账户勉强可以**（内存98.3%，不推荐）
- ❌ **10组账户无法运行**（内存和CPU都超载）

---

### 优化后配置

**优化措施**：
1. 关闭不必要的Windows服务（释放 ~200 MB）
2. 清理临时文件和缓存（释放 ~100 MB）
3. 优化Python内存管理（释放 ~50 MB）
4. 调整PostgreSQL内存配置（释放 ~50 MB）

**优化后可用内存**：610 MB

| 账户组数 | 内存使用 | CPU使用（空闲） | CPU使用（交易） | 状态 |
|---------|---------|----------------|----------------|------|
| 1组（当前） | 88.5% | 32% | 50% | ✅ 正常 |
| 5组 | 93.3% | 60% | 85% | ✅ 可用 |
| 8组 | 96.8% | 75% | 105% | ⚠️ CPU超载 |
| 10组 | 99.2% | 90% | 130% | ❌ CPU超载 |

**结论**：
- ✅ **优化后最多支持 5-7组账户**（安全运行）
- ⚠️ **8组账户可能CPU超载**（交易时105%）
- ❌ **10组账户CPU严重超载**（130%）

---

## 瓶颈分析

### 主要瓶颈：内存

**当前状态**：
- 总内存：4 GB
- 已用：93.5%
- 可用：260 MB（6.5%）

**瓶颈原因**：
1. Windows Server 2022基础占用大（~2.2 GB）
2. Python进程内存占用（~300 MB）
3. PostgreSQL内存占用（~300 MB）
4. MT5客户端内存占用（31 MB/个）

**解决方案**：
1. **短期**：优化现有内存使用（可释放 ~350 MB）
2. **中期**：升级到 8GB 内存实例（如 c6i.xlarge）
3. **长期**：使用Linux系统（节省 ~500 MB系统内存）

---

### 次要瓶颈：CPU

**当前状态**：
- 总CPU：2 vCPU
- 空闲使用：32%
- 交易使用：50%

**瓶颈原因**：
1. 多个MT5客户端同时运行（5-10% CPU/个）
2. 多个策略同时执行（5-8% CPU/组）
3. Python GIL限制（单线程性能）

**解决方案**：
1. **短期**：优化策略执行逻辑（减少CPU密集操作）
2. **中期**：升级到 4 vCPU实例（如 c6i.xlarge）
3. **长期**：使用多进程架构（绕过GIL限制）

---

## 升级方案对比

### 方案A：保持当前配置（c6i.large）

**容量**：
- 未优化：3-4组账户
- 优化后：5-7组账户

**成本**：
- 实例费用：~$0.085/小时
- 月成本：~$62

**优点**：
- ✅ 无需迁移
- ✅ 成本最低

**缺点**：
- ❌ 容量有限
- ❌ 扩展性差
- ❌ 内存压力大

---

### 方案B：升级到 c6i.xlarge

**配置**：
- CPU：4 vCPU
- 内存：8 GB
- 网络：最高 12.5 Gbps

**容量**：
- 预估：15-20组账户

**成本**：
- 实例费用：~$0.17/小时
- 月成本：~$124
- 增加成本：+$62/月

**优点**：
- ✅ 容量翻倍
- ✅ 性能提升
- ✅ 扩展性好

**缺点**：
- ❌ 成本增加100%
- ⚠️ 需要迁移

---

### 方案C：升级到 c6i.2xlarge

**配置**：
- CPU：8 vCPU
- 内存：16 GB
- 网络：最高 12.5 Gbps

**容量**：
- 预估：40-50组账户

**成本**：
- 实例费用：~$0.34/小时
- 月成本：~$248
- 增加成本：+$186/月

**优点**：
- ✅ 容量充足
- ✅ 性能强劲
- ✅ 长期扩展

**缺点**：
- ❌ 成本增加300%
- ⚠️ 需要迁移
- ⚠️ 可能过度配置

---

### 方案D：迁移到Linux（c6i.large）

**配置**：
- CPU：2 vCPU
- 内存：4 GB
- 操作系统：Ubuntu 22.04

**容量**：
- 预估：8-10组账户

**成本**：
- 实例费用：~$0.085/小时
- 月成本：~$62
- 增加成本：$0

**优点**：
- ✅ 成本不变
- ✅ 内存节省 ~500 MB
- ✅ 性能提升

**缺点**：
- ❌ 需要重新部署
- ❌ MT5需要Wine或虚拟化
- ❌ 兼容性问题

---

## 推荐方案

### 短期方案（1-2周）：优化现有系统

**目标**：支持 5组账户

**步骤**：
1. **内存优化**：
   - 关闭不必要的Windows服务
   - 清理临时文件
   - 优化Python内存管理
   - 调整PostgreSQL配置

2. **监控部署**：
   - 添加内存使用监控
   - 添加CPU使用监控
   - 设置告警阈值

3. **逐步扩展**：
   - 先添加到3组，观察1周
   - 再添加到5组，观察1周
   - 根据监控数据调整

**预期效果**：
- ✅ 支持5组账户
- ✅ 内存使用 ~93%
- ✅ CPU使用 ~85%（交易时）

---

### 中期方案（1-2个月）：升级实例

**推荐**：升级到 **c6i.xlarge**（4 vCPU, 8 GB）

**理由**：
1. 容量翻倍（15-20组账户）
2. 成本可控（+$62/月）
3. 性能提升明显
4. 扩展性好

**迁移步骤**：
1. 创建新实例快照
2. 启动c6i.xlarge实例
3. 恢复数据和配置
4. 测试验证
5. 切换DNS
6. 停止旧实例

**预期效果**：
- ✅ 支持15-20组账户
- ✅ 内存使用 ~60%
- ✅ CPU使用 ~50%（交易时）

---

### 长期方案（3-6个月）：架构优化

**目标**：支持 50+组账户

**架构改进**：
1. **微服务化**：
   - 市场数据服务独立
   - 策略执行服务独立
   - MT5连接服务独立

2. **负载均衡**：
   - 多个后端实例
   - 数据库读写分离
   - Redis缓存层

3. **容器化**：
   - Docker部署
   - Kubernetes编排
   - 自动扩缩容

**预期效果**：
- ✅ 支持50+组账户
- ✅ 高可用性
- ✅ 弹性扩展

---

## 立即可行的优化措施

### 1. 关闭不必要的Windows服务

```powershell
# 停止Windows Search（节省 ~50 MB）
Stop-Service -Name "WSearch" -Force
Set-Service -Name "WSearch" -StartupType Disabled

# 停止Windows Update（节省 ~30 MB）
Stop-Service -Name "wuauserv" -Force
Set-Service -Name "wuauserv" -StartupType Disabled

# 停止Superfetch（节省 ~50 MB）
Stop-Service -Name "SysMain" -Force
Set-Service -Name "SysMain" -StartupType Disabled

# 停止Print Spooler（节省 ~20 MB）
Stop-Service -Name "Spooler" -Force
Set-Service -Name "Spooler" -StartupType Disabled
```

**预期释放**：~150 MB

---

### 2. 清理临时文件

```powershell
# 清理Windows临时文件
Remove-Item -Path "C:\Windows\Temp\*" -Recurse -Force -ErrorAction SilentlyContinue

# 清理用户临时文件
Remove-Item -Path "$env:TEMP\*" -Recurse -Force -ErrorAction SilentlyContinue

# 清理日志文件
Remove-Item -Path "C:\app\hustle2026\backend\*.log" -Force -ErrorAction SilentlyContinue
```

**预期释放**：~100 MB

---

### 3. 优化PostgreSQL配置

```ini
# postgresql.conf
shared_buffers = 128MB  # 从默认256MB降低
work_mem = 4MB          # 从默认8MB降低
maintenance_work_mem = 64MB  # 从默认128MB降低
max_connections = 20    # 从默认100降低
```

**预期释放**：~50 MB

---

### 4. 优化Python内存

```python
# 在main.py中添加
import gc
gc.set_threshold(700, 10, 10)  # 更激进的垃圾回收

# 定期清理
async def cleanup_memory():
    while True:
        await asyncio.sleep(300)  # 每5分钟
        gc.collect()
```

**预期释放**：~50 MB

---

## 总结

### 当前配置（c6i.large, 4GB RAM）

**未优化**：
- ✅ 最多支持 **3-4组双平台账户**
- ⚠️ 5组账户勉强可以（内存98.3%）
- ❌ 无法支持10组账户

**优化后**：
- ✅ 最多支持 **5-7组双平台账户**
- ⚠️ 8组账户CPU可能超载
- ❌ 无法支持10组账户

---

### 升级后配置（c6i.xlarge, 8GB RAM）

**容量**：
- ✅ 最多支持 **15-20组双平台账户**
- ✅ 内存使用 ~60%
- ✅ CPU使用 ~50%

**成本**：
- 月成本：$124（+$62）
- 性价比：高

---

### 建议

1. **立即行动**：
   - 优化现有系统（释放 ~350 MB内存）
   - 添加监控告警
   - 逐步扩展到5组账户

2. **1个月内**：
   - 如果需要超过5组账户，升级到c6i.xlarge
   - 成本增加可控（+$62/月）
   - 容量提升3-4倍

3. **3个月内**：
   - 评估长期需求
   - 考虑架构优化
   - 规划容器化部署

**最终答案**：
- 当前配置：**最多4组账户**（安全）
- 优化后：**最多7组账户**（安全）
- 升级后：**最多20组账户**（c6i.xlarge）
