<Window x:Class="QhClient.WebShellWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Quant Hedge 交易终端" Height="820" Width="1280"
        MinHeight="600" MinWidth="900"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen" Background="#0B1220"
        SizeChanged="Window_SizeChanged">
  <Grid>
    <!-- 真实 dashboard -->
    <wv2:WebView2 x:Name="Web"/>

    <!-- 首帧防白屏 / 断线重连 覆盖层(原生绘制, 不依赖 web) -->
    <Border x:Name="Overlay" Background="#0B1220" Visibility="Visible">
      <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock Text="QUANT HEDGE" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
        <TextBlock x:Name="OverlayMsg" Text="正在连接交易终端…" FontSize="13" Foreground="#7C8B9E" HorizontalAlignment="Center" Margin="0,10,0,0"/>
        <ProgressBar x:Name="OverlayBar" IsIndeterminate="True" Width="220" Height="4" Margin="0,16,0,0" Background="#111A2E" Foreground="#2E8BD6" BorderThickness="0"/>
        <Button x:Name="RetryBtn" Content="重试" Visibility="Collapsed" Click="Retry_Click" Margin="0,18,0,0" Padding="24,7"
                Background="#2E8BD6" Foreground="White" BorderThickness="0" Cursor="Hand" HorizontalAlignment="Center"/>
      </StackPanel>
    </Border>
  </Grid>
</Window>
